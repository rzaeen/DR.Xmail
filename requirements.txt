# DR.Xmail — runtime deps
# minimal: only cryptography is required for HTTP Signatures / Fediverse delivery
cryptography>=42.0.0
requests>=2.31.0
# optional UI / API:
# streamlit>=1.30.0
# fastapi>=0.110.0
# uvicorn>=0.27.0
